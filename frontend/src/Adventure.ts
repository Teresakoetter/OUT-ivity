export type Adventure = {
    id: string,
    name: string,
    quote: string,
    description: string
    url: string
}

export type NewAdventure = {
    name: string,
    quote: string,
    description: string
}